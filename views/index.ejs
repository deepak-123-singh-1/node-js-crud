<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
</head>
<body>
    <h1>Student List</h1>
    <a href="/create">Add Student Form</a>
    <ul>
        <% student.forEach((stud, index) => { %>
            <li>
                <%= stud %>
            </li>
        <% }) %>
    </ul>
</body>
</html>